using UnityEngine;

public class Grid : MonoBehaviour
{

    void Start()
    {
        
    }

    void Update()
    {
       /* if (Input.GetMouseButtonDown(0))
        {
            isMousePressed = true;
            mousePressedTime = Time.time;
        }

        if (isMousePressed && Input.GetMouseButton(0))
        {
            if (Time.time - mousePressedTime > holdDuration)
            {
                // Действие, которое нужно выполнить при удержании кнопки более секунды
                Debug.Log("Кнопка удерживается более секунды!");
            }
        }

        if (Input.GetMouseButtonUp(0))
        {
            isMousePressed = false;
        }*/
    }
}
